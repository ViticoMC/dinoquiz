import { Inter } from 'next/font/google';

// Configura la fuente (puedes elegir pesos: 400, 500, 600, 700, etc.)
export const inter = Inter({ 
  subsets: ['latin'],
  weight: ['700'], // Peso bold para encabezados
});